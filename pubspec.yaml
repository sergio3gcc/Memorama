name: memorama
description: A new Flutter project.

#publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  #sqflite: ^2.0.0+4
  #path_provider: ^2.0.2
  english_words: ^4.0.0
  provider: ^6.0.0
  audioplayers: ^6.1.0
  sqflite: ^2.0.0+3
  path: ^1.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/sounds/matching_sound.mp3
